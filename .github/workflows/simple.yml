name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eccho a string
        run: echo "Hello World!"
      - name: multiline string
        run: |
          node -v
          npm -v